---
version: 2

models:
  - name: stg_live_timing__heartbeat
    description: '{{ doc("tbl_stg_live_timing__heartbeat") }}'
    meta:
      dagster:
        asset_key:
          - staging
          - live_timing
          - heartbeat
