---
version: 2

models:
  - name: stg_live_timing__weather_data
    description: '{{ doc("tbl_stg_live_timing__weather_data") }}'
    meta:
      dagster:
        asset_key:
          - staging
          - live_timing
          - weather_data
