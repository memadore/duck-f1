---
version: 2

models:
  - name: stg_live_timing__index
    description: '{{ doc("tbl_stg_live_timing__index") }}'
    meta:
      dagster:
        asset_key:
          - staging
          - live_timing
          - index
