---
version: 2

models:
  - name: stg_live_timing__driver_list
    description: '{{ doc("tbl_stg_live_timing__driver_list") }}'
    meta:
      dagster:
        asset_key:
          - staging
          - live_timing
          - driver_list
