---
version: 2

models:
  - name: stg_live_timing__sessions
    description: '{{ doc("tbl_live_timing__sessions") }}'
    columns:
      - name: event_key
        tests:
          - not_null
      - name: session_key
        tests:
          - unique
          - not_null
