---
version: 2
models:
  - name: rpt_driver__stats_seasons
    description: '{{ doc("tbl_rpt_driver__stats_seasons") }}'
    config:
      alias: driver__stats_seasons
    meta:
      dagster:
        asset_key:
          - reports
          - drivers
          - stats_seasons
    columns:
      - name: driver_id
        tests:
          - not_null
