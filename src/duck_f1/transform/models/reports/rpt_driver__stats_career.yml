---
version: 2
models:
  - name: rpt_driver__stats_career
    description: '{{ doc("tbl_rpt_driver__stats_career") }}'
    config:
      alias: driver__stats_career
    meta:
      dagster:
        asset_key:
          - reports
          - drivers
          - stats_career
    columns:
      - name: driver_id
        tests:
          - unique
          - not_null
