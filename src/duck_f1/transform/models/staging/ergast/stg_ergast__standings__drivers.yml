---
version: 2
models:
  - name: stg_ergast__standings__drivers
    description: '{{ doc("tbl_stg_ergast__standings__drivers") }}'
    meta:
      dagster:
        asset_key:
          - staging
          - ergast
          - standings
          - driver
