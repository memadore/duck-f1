---
version: 2
models:
  - name: stg_live_timing__session_info
    description: '{{ doc("tbl_stg_live_timing__session_info") }}'
    meta:
      dagster:
        asset_key:
          - staging
          - live_timing
          - session_info
