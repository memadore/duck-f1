---
version: 2
models:
  - name: stg_live_timing__events
    description: '{{ doc("tbl_stg_live_timing__sessions") }}'
    meta:
      dagster:
        asset_key:
          - staging
          - live_timing
          - events
    columns:
      - name: event_id
      - name: live_timing_event_sha
      - name: season
      - name: round
      - name: event_name
      - name: event_official_name
      - name: event_country
      - name: event_location
