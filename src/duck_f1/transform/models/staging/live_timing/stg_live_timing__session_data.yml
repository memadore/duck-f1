---
version: 2
models:
  - name: stg_live_timing__session_data
    description: '{{ doc("tbl_stg_live_timing__session_data") }}'
    meta:
      dagster:
        asset_key:
          - staging
          - live_timing
          - session_data
