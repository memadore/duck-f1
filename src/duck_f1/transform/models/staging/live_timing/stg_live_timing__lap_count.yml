---
version: 2
models:
  - name: stg_live_timing__lap_count
    description: '{{ doc("tbl_stg_live_timing__lap_count") }}'
    meta:
      dagster:
        asset_key:
          - staging
          - live_timing
          - lap_count
