---
version: 2
models:
  - name: stg_live_timing__current_tyres
    description: '{{ doc("tbl_stg_live_timing__current_tyres") }}'
    meta:
      dagster:
        asset_key:
          - staging
          - live_timing
          - current_tyres
