---
version: 2
models:
  - name: mart__dim_circuits
    description: '{{ doc("tbl_mart__dim_circuits") }}'
    config:
      alias: dim_circuits
    meta:
      dagster:
        asset_key:
          - mart
          - dim
          - circuits
    columns:
      - name: circuit_id
        tests:
          - unique
          - not_null
