---
version: 2
models:
  - name: mart__fct_session__weather
    description: '{{ doc("tbl_mart__fct_session__weather") }}'
    config:
      alias: fct_session__weather
    meta:
      dagster:
        asset_key:
          - mart
          - fact
          - session
          - weather
    columns:
      - name: session_id
        tests:
          - not_null
          - relationships:
              to: ref('mart__dim_sessions')
              field: session_id
