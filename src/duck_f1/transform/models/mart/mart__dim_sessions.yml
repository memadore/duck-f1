---
version: 2
models:
  - name: mart__dim_sessions
    description: '{{ doc("tbl_mart__dim_races") }}'
    config:
      alias: dim_sessions
    meta:
      dagster:
        asset_key:
          - mart
          - dim
          - sessions
    columns:
      - name: session_id
        tests:
          - unique
          - not_null
