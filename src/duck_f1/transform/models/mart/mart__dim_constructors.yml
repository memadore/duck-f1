---
version: 2
models:
  - name: mart__dim_constructors
    description: '{{ doc("tbl_mart__dim_constructors") }}'
    config:
      alias: dim_constructors
    meta:
      dagster:
        asset_key:
          - mart
          - dim
          - constructors
    columns:
      - name: constructor_id
        tests:
          - unique
          - not_null
