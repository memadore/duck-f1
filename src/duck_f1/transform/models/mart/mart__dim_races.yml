---
version: 2
models:
  - name: mart__dim_races
    description: '{{ doc("tbl_mart__dim_races") }}'
    config:
      alias: dim_races
    meta:
      dagster:
        asset_key:
          - mart
          - dim
          - races
    columns:
      - name: race_id
        tests:
          - unique
          - not_null
