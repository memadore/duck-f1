---
version: 2
models:
  - name: mart__dim_events
    description: '{{ doc("tbl_mart__dim_races") }}'
    config:
      alias: dim_events
    meta:
      dagster:
        asset_key:
          - mart
          - dim
          - events
    columns:
      - name: event_id
        tests:
          - unique
          - not_null
