---
version: 2
models:
  - name: mart__fct_standings__drivers
    description: '{{ doc("tbl_mart__fct_standings__drivers") }}'
    config:
      alias: fct_standings__drivers
    meta:
      dagster:
        asset_key:
          - mart
          - fact
          - standings
          - drivers
    tests:
      - unique:
          column_name: "race_id || '-' || driver_id"
          config:
            severity: warn # TODO: investigate
    columns:
      - name: driver_id
        tests:
          - not_null
          - relationships:
              to: ref('mart__dim_drivers')
              field: driver_id
      - name: race_id
        tests:
          - not_null
          - relationships:
              to: ref('mart__dim_races')
              field: race_id
