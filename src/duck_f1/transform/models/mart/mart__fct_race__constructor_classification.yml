---
version: 2
models:
  - name: mart__fct_race__constructor_classification
    description: '{{ doc("tbl_mart__fct_race__constructor_classification") }}'
    config:
      alias: fct_race__constructor_classification
    meta:
      dagster:
        asset_key:
          - mart
          - fact
          - race
          - constructor_classification
    tests:
      - unique:
          column_name: "session_id || '-' || constructor_id"
    columns:
      - name: session_id
        tests:
          - not_null
          - relationships:
              to: ref('mart__dim_races')
              field: session_id
      - name: constructor_id
        tests:
          - not_null
          - relationships:
              to: ref('mart__dim_constructors')
              field: constructor_id
