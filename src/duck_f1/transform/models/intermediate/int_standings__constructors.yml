---
version: 2
models:
  - name: int_standings__constructors
    description: '{{ doc("tbl_int_standings__constructors") }}'
    meta:
      dagster:
        asset_key:
          - intermediate
          - standings
          - constructors
