---
version: 2
models:
  - name: int_live_timing__lap__sector_times
    description: '{{ doc("tbl_int_live_timing__lap__sector_times") }}'
    meta:
      dagster:
        asset_key:
          - intermediate
          - live_timing
          - lap
          - sector_times
    columns:
      - name: session_id
      - name: car_number
      - name: lap_number
      - name: sector_key
      - name: sector_time
      - name: session_ts
