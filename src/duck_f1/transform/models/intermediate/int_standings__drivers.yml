---
version: 2
models:
  - name: int_standings__drivers
    description: '{{ doc("tbl_int_standings__drivers") }}'
    meta:
      dagster:
        asset_key:
          - intermediate
          - standings
          - drivers
