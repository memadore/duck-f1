---
version: 2
models:
  - name: int_live_timing__car__positions
    description: '{{ doc("tbl_int_live_timing__car__positions") }}'
    meta:
      dagster:
        asset_key:
          - intermediate
          - live_timing
          - car
          - positions
    columns:
      - name: session_id
      - name: car_number
      - name: lap_number
      - name: session_ts
      - name: car_status
      - name: x_position
      - name: y_position
      - name: z_position
